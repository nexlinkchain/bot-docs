{
  "name": "bot-docs",
  "version": "1.0.0",
  "private": true,
  "description": "NexLink Bot API documentation (HonKit / GitBook)",
  "scripts": {
    "build": "honkit build . _book",
    "serve": "honkit serve"
  },
  "devDependencies": {
    "honkit": "^6.0.0"
  }
}
